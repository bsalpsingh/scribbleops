apiVersion: apps/v1
kind: Deployment
metadata:
  name: scribble-backend-prod-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scribble-backend-prod
  template:
    metadata:
      labels:
        app: scribble-backend-prod
    spec:
      containers:
      - name: scribble-backend-prod
        image: bsalsingh/scribble-api:1753113931
        ports:
        - containerPort: 8080
        envFrom:
        - secretRef:
            name: scribble-secret
